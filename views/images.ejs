<html>

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Images Info</title>
    <style>
        table,
        th,
        tr,
        td {
            border: 1px solid rgb(255, 0, 0);
            font-size: large;
        }
    </style>
</head>

<body>
    <% i=1 %>
    <% if (imgPathArr.length>0) { %>
    <h1><u> There are <%=imgPathArr.length/7 %> uploaded images </u></h1>
    <table style="width:auto">
        <tr>
            <th>Original image</th>
            <th>500*500</th>
            <th>250*250</th>
            <th>Blurred</th>
            <th>Sharped</th>
            <th>Gray</th>
            <th>Rotated by 180</th>
        </tr>
        <tr>
            <% imgPathArr.forEach((imgPath) => { %>
            <td>
                <img src="/<%=imgPath %>" />
            </td>
            <% if (i==7) { %>
        </tr>
        <tr>
            <% i=0 %>
            <% } %>
            <% i++ %>
            <% }) %>
    </table>
    <% } else {%>
    <h1>No images are uploaded yet !</h1>
    <% }%>
    <h3> <a href="/uploadImage"> Click here to upload an image</a> </h3>
</body>

</html>